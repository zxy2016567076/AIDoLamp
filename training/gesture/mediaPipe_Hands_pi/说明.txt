我本来的程序是可以识别到10个我定义的手势，识别到分别在屏幕上显示手势对应的数字其中它们的对应关系是CLASS_MAPPING = {
        1: 'activate', 2: 'down', 3: 'exit', 4: 'left',
        5: 'mode1', 6: 'mode2', 7: 'mode3', 8: 'mode4',
        9: 'right', 10: 'up'
    }
   例如其中的activate是我的文件夹，里面存放我的手势照片用来训练，但是我的主程序只能在识别到这些手势后在屏幕上显示对应的编号（数字），我想要主函数的功能更加完善，比如可以实现：
编号为：1的手势作为开启手势调节的开关，需要识别到这个手势3秒，才能开启用手势调节，在电脑识别到编号为1的手势3秒后，然后分别用编号为5的手势作为模式1，编号为6的手势作为模式2，编号为7的手势作为模式3，编号为8的手势作为模式4，分别表示四个模式，识别编号为5的手势后，进入模式1，代表开启第一个模式，现在在程序里面，进入模式一后，识别到编号为10的手势，进行一些操作（在代码中可以用输出1（print("1")）来代替），然后识别到编号为2的手势，则输出2，识别到编号为9的手势则输出3，识别到编号为4的手势输出4，然后，有一个退出的手势，它的编号是3，退出后可以再通过手势识别到编号为6的的手势，进入第二个模式，控制也和第一个模式一样分别用编号为10.2.9.4的手势，识别到后分别输出（1.2.3.4）如果不想手动控制了，再执行编号为3的手势退出就行了，（退出后再想进入就还得识别编号为1的手势3秒）以此类推，编号为7的手势就是进入模式3，编号为8的手势就是进入模式4，控制都是一样的，我需要确保主函数可以实现这个功能，其中这些是我写的，特征提取程序，还有训练程序，和主函数


优势
1.防抖机制：

模式内操作间隔至少1秒

激活需要持续检测手势

2.新增状态管理系统：

activated: 系统总开关状态

current_mode: 当前所处模式（1-4）

activation_start_time: 用于检测持续3秒激活

last_action_time: 操作防抖计时

3.智能计时器管理：

自动清除非当前手势的计时器

完成操作后自动清除相关计时器

切换手势时自动重置之前的计时

4.防误触机制

模式控制操作1秒防抖间隔
手势切换需要经历完整计时周期
关键状态切换后重置计时器



技术总结
核心技术栈：

MediaPipe：用于实时手部关键点检测（21个三维坐标点）

随机森林模型：基于63维特征（21关键点×3坐标）的手势分类

OpenCV：视频流处理、图像渲染和可视化交互

状态机设计：通过activated和current_mode管理多级控制逻辑

关键算法：

动态时间验证机制（1秒/3秒持续识别）

手势生命周期管理（计时器自动重置）

操作防抖设计（1秒动作冷却时间）

程序优势
强鲁棒性：

通过持续时长验证（1秒/3秒）显著降低误触率

采用随机森林模型，对特征尺度变化具有天然抗干扰能力

多级交互设计：

三级状态流转：未激活 → 激活 → 模式控制

支持4个可扩展模式，每个模式独立操作逻辑

实时反馈系统：

动态进度条可视化识别进度

界面实时显示系统状态（激活/模式/当前手势）

工程化特性：

模型与业务逻辑解耦，便于模型替换升级

全流程错误处理（无效图像跳过、异常状态恢复）

核心难点
时间敏感状态管理：

需精确协调多个计时器（激活/操作/模式切换）

处理手势中断时的状态回滚（如未达时长时切换手势）

实时性优化：

在30FPS视频流中平衡计算开销（MediaPipe检测+模型预测）

关键点坐标抖动带来的特征波动（需模型具备容错性）

多手势冲突处理：

防止模式切换手势（5-8）与操作手势（2/4/9/10）的误判

退出手势（3）在全局系统和当前模式中的双重作用

用户体验平衡：

持续时长验证带来的操作延迟与交互流畅度的矛盾

视觉反馈（进度条）的平滑过渡与计算资源消耗